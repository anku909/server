{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "index.js", "headers": { "Timeout": "30s" } },
    { "src": "/(.*)", "dest": "index.js" }
  ]
}
